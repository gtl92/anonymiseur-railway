# Dépendances système pour le package npm "canvas" (rendu PDF → image pour OCR)
[phases.setup]
nixPkgs = [
  "cairo",
  "pango",
  "giflib",
  "libjpeg",
  "librsvg",
  "pixman",
  "pkg-config",
  "python3"   # requis par node-gyp pour compiler canvas
]
